A parser with the exact same grammar of the real parser, but dumb mocked objects behind.

Node instancier and nodes are faked and do barely nothing, to see if the problem comes from the grammar only.
